# Copy this file to orders-api.env and fill in real values.
# orders-api.env is gitignored and is never printed to Jenkins console output
# (Phase 7: "Do not expose secrets in logs"). Jenkins populates the real file
# from a Jenkins Credentials binding at deploy time, not from source control.
ORDERS_API_SECRET=replace-with-a-real-secret
